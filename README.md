# Hello Angular (Minimal)

This is a minimal Angular app scaffold that displays "Hello World".

Requirements:
- Node.js and npm (recent versions)

Install and run:

```bash
npm install
npm start
```

The `npm start` script runs the Angular dev server and opens the app in your browser.
